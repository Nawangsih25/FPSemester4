import { ComponentFixture, TestBed } from '@angular/core/testing';
import { HasilTransferAngsuranPage } from './hasil-transfer-angsuran.page';

describe('HasilTransferAngsuranPage', () => {
  let component: HasilTransferAngsuranPage;
  let fixture: ComponentFixture<HasilTransferAngsuranPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(HasilTransferAngsuranPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
